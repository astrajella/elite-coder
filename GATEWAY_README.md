# Gateway README - proxy rules
This repository provides a simple gateway pattern. Run services and configure Next.js to proxy API routes to the appropriate service ports:
- /api/agent -> http://localhost:8001
- /api/retrieval -> http://localhost:8002
- /api/ledger -> http://localhost:8003
